---
apiVersion: v1
kind: Service
metadata:
  labels:
    app: artsyapp
    component: web
    layer: application
  name: web-internal
  namespace: default
spec:
  ports:
  - port: 8080
    protocol: TCP
    name: http
    targetPort: http
  selector:
    app: artsyapp
    layer: application
    component: web
  type: ClusterIP
